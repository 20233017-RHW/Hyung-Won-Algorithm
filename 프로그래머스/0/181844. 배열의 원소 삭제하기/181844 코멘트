두개의 배열중 같은 값을 삭제하여 다시 돌려주는 문제
배열의 최대크기가 100까지밖에 되지 않기때문에 이차원반복을 써도 풀리긴 했을거같지만 문제에 중복값이 없다는 조건이 있기 때문에
해쉬셋을 사용하여 해결
해쉬셋 = 중복허용x, 순서없음, 검색속도 빠름
그리고 c++에서는 늘 vector를 사용했지만 자바에서는 ArrayList를 사용해야하는듯함
ArrayList = 벡터와 비슷한 기능을하는 동적 배열이지만 add()로 넣고 get()으로 접근 remove()로 삭제
다만 처음에는 import java.util.*; 이걸쓰지않아 오류
해쉬맵, 해쉬셋, 동적배열등을 사용하려면 자바에서는 코드 첫줄에 이게 반드시 필요
