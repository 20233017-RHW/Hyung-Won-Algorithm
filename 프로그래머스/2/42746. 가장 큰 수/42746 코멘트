배열에 들어있는 숫자들을 조합하여 나올수있는 숫자중 가장 큰 수를 찾는 문제
숫자를 문자열로 변환한 후 정렬하는 과정에서 더 큰 숫자가 앞으로 오는 방법을 사용
sort(v.begin(), v.end(), [](string a, string b) {
    return a + b > b + a;
});
이러한 형태로 만드는 경우 정렬하는 과정에서 문자열을 이어붙였을때 더 큰수가 될수있도록 
숫자들을 정렬한 형태로 제공
