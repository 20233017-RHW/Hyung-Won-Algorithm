절대값이 작은 수부터 출력하되, 같은수라면 실제값이 작은 수부터 나오는 절대값 힙을 만드는 문제
우선순위큐의 오름차순 기능을 떠올려서 
bool operator()(int a, int b) {
		if (abs(a) == abs(b)) {
			return a > b;
		}
		return abs(a) > abs(b);
	} 이렇게 비교하는 구조체를 만들어 절대값이 같다면 작은 수의 우선순위를 올리는 방법을 사용
